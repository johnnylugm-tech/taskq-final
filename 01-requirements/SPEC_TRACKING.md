# Specification Tracking Matrix — taskq-api

> On-demand Lazy Load template.
> Human-readable view over SPEC.md (root) — authoritative state lives in TRACEABILITY_MATRIX.md and quality_manifest.json.

## Project Info

- Project Name: taskq-api
- Version: v1.0.0
- Created: 2026-09-02
- Canonical spec source: `SPEC.md` (repo root, v1.0.0, 2026-07-30)
- Compiled from: `01-requirements/SRS.md` (Phase 1 deliverable, APPROVED)

## Specification Status

> **The Status column is machine-refreshed** — `advance-phase` overwrites each
> FR's Status from `build_traceability`'s live code/test scan (IN_PROGRESS once
> code/module exists, VERIFIED once code+test exist). The authoritative status is
> that scan / `quality_manifest.json`, NOT this hand-filled cell. Fill the
> semantic columns (Spec Description / Intent Class / Decision Framework /
> Owner / Notes); leave Status to refresh itself (a hand-edit is overwritten on
> the next advance).
>
> **Status legend**: `DRAFT` = not yet encountered by build_traceability;
> `IN_PROGRESS` = code/module present; `VERIFIED` = code+test passing;
> `BLOCKED` = dependency pending downstream deliverable.

### Functional Requirements (FR — SPEC.md §3)

| FR ID | Spec Description | Intent Class | Decision Framework | Owner | Status | Notes |
|-------|-----------------|--------------|--------------------|-------|--------|-------|
| FR-01 | Task resource CRUD API — POST/GET/DELETE `/v1/tasks` with cursor-based pagination (no offset), `TaskCreate` validation, 422 on validation failure, 404 on unknown id; spec lines 79–92. | behavioral / api-surface | Cursor pagination is mandated (not offset) to avoid full-table scans on `/v1/tasks` — directly couples to NFR-01 N+1 guard. Validation is Pydantic-bound at the API boundary; errors must surface as `application/problem+json` per FR-10. | Agent A · REQ | VERIFIED | Source: `SPEC.md` (root) lines 79–92. ACs: AC-1.1–AC-1.5 in `01-requirements/SRS.md` §3. Companion deliverable: `01-requirements/TRACEABILITY_MATRIX.md`. |
| FR-02 | Task execution endpoint — `POST /v1/tasks/{id}/run` returns 202 + `run_id`; subprocess via `asyncio.create_subprocess_exec` with `shlex.split` (no `shell=True`); status machine `pending → running → done \| failed \| timeout`; results persisted to `task_results`; history endpoint `GET /v1/tasks/{id}/runs`; spec lines 93–100. | behavioral / async-runtime | Subprocess discipline is NFR-02's anti-pattern gate; the executor's coroutine boundary is the only place where process termination is enforced (FR-08). Status machine endpoints must produce RFC 7807 errors on illegal transitions. | Agent A · REQ | IN_PROGRESS | Source: `SPEC.md` (root) lines 93–100. ACs: AC-2.1–AC-2.5 in `01-requirements/SRS.md` §3. Couples to FR-07 v3 schema (results table) and FR-08 executor. |
| FR-03 | API Key authentication — all `/v1/*` endpoints require `X-API-Key`; SHA-256 hashed storage (never plaintext); constant-time compare via `hmac.compare_digest`; plaintext emitted only once at `key create`; revoked keys (`revoked_at` non-null) invalid; `/healthz` and `/readyz` exempt; spec lines 101–107. | security / identity | SHA-256 + `hmac.compare_digest` is the canonical password-storage discipline applied to API keys; NFR-02 AC-N2.3 enforces both. Single-emission of plaintext is a process invariant — any test harness that re-emits it must fail. | Agent A · REQ | IN_PROGRESS | Source: `SPEC.md` (root) lines 101–107. ACs: AC-3.1–AC-3.5 in `01-requirements/SRS.md` §3. Migration v1 introduces `api_keys` table per FR-07. |
| FR-04 | Scope authorization — `read < write < admin` (hierarchical inclusion); required scope per endpoint (FR-01/02 table); insufficient scope → 403 + problem+json (`/errors/forbidden`), body MUST NOT disclose resource existence; authorization enforced in a single dependency, not scattered in handlers; spec lines 109–113. | security / authorization | Single-dependency discipline is a testable invariant ("every `/v1` route shares one dep") — FR-04 AC-4.3 is the structural assertion. Information-disclosure (403 vs 404 boundary) maps to NFR-02 AC-N2.4. | Agent A · REQ | VERIFIED | Source: `SPEC.md` (root) lines 109–113. ACs: AC-4.1–AC-4.3 in `01-requirements/SRS.md` §3. Couples to FR-03 (key lookup) and FR-10 (error shape). |
| FR-05 | Rate control — per-token token bucket (capacity `TASKQ_RATE_BURST`, refill `TASKQ_RATE_PER_SEC`); over-limit → 429 + problem+json (`/errors/rate-limited`) + `Retry-After` header (seconds); bucket state persisted in `rate_buckets` table; updates single-transaction with row-level lock; `/healthz` and `/readyz` exempt; spec lines 115–120. | reliability / backpressure | Persisted bucket is the only choice for cross-worker consistency; row-level lock is the only safe primitive against the R12 race (see `01-requirements/SRS.md` §8). Token-bucket math is the unit-testable kernel. | Agent A · REQ | VERIFIED | Source: `SPEC.md` (root) lines 115–120. ACs: AC-5.1–AC-5.4 in `01-requirements/SRS.md` §3. Migration v1 introduces `rate_buckets` table; cross-cuts NFR-01 (token lookup latency). |
| FR-06 | Persistence layer & transaction boundaries — all data access via `repository/`; service layer does not hold `Session` directly; per-request Session via context manager (commit on success, rollback on exception); ORM / parameterized queries only (no string-concat SQL); eager loading via `selectinload` / `joinedload` (N+1 is a fail condition); connection pool `pool_size=TASKQ_DB_POOL_SIZE` (default 5) with `pool_pre_ping=True`; spec lines 122–128. | architectural / data-integrity | The repository-only-on-Session rule is the first half of NFR-06's `import-linter` contract (the second half is "no `sqlalchemy` outside `repository/`"). Context-manager-bound transactions are the only way to satisfy both FR-06 and NFR-03 AC-N3.1 without hand-rolled try/finally. | Agent A · REQ | VERIFIED | Source: `SPEC.md` (root) lines 122–128. ACs: AC-6.1–AC-6.5 in `01-requirements/SRS.md` §3. STRUCTURAL GATE: NFR-06 AC-N6.1 (lint-imports exit 0) cannot pass without this FR. |
| FR-07 | Alembic schema migration in three revisions — v1 (`tasks`, `api_keys`), v2 (add `tags`, `task_tags`, unique index on `tasks.name`), v3 (split `tasks.result_json` → `task_results` with data migration); each revision reversible; `upgrade head` ↔ `downgrade base` round-trip preserves data field-by-field; destructive shortcuts (`op.execute("DROP TABLE ...")`) prohibited; migrations covered by offline-SQL test; spec lines 130–143. | data-integrity / migration | v3 data migration is the highest-risk slice (R1 in `01-requirements/SRS.md` §8). NFR-09 AC-N9.5 forbids skipping this FR on "too hard to test" grounds; the round-trip test must run against a real SQLite file, not an in-memory mock. Downgrades that drop tables without re-merging data violate AC-7.3. | Agent A · REQ | VERIFIED | Source: `SPEC.md` (root) lines 130–143. ACs: AC-7.1–AC-7.7 in `01-requirements/SRS.md` §3. Forward-coupled to FR-02 (results table consumer), FR-03/FR-05 (v1 tables). |
| FR-08 | Async executor — `asyncio.TaskGroup`; graceful drain on shutdown (wait in-flight tasks up to `TASKQ_DRAIN_TIMEOUT`, then mark `interrupted`); concurrency cap `TASKQ_MAX_CONCURRENT` (default 8), excess tasks queue not spawn unboundedly; timeout via `asyncio.wait_for` followed by `process.kill()` + `await process.wait()` (no orphan processes); `asyncio.CancelledError` must propagate; spec lines 145–150. | reliability / async-runtime | Process kill discipline is the only answer to FR-08 timeout orphans (R8 in `01-requirements/SRS.md` §8). `asyncio.CancelledError` propagation is NFR-03 AC-N3.3 — a swallowed cancel is the canonical async pitfall the harness explicitly tests for. | Agent A · REQ | VERIFIED | Source: `SPEC.md` (root) lines 145–150. ACs: AC-8.1–AC-8.4 in `01-requirements/SRS.md` §3. Couples to FR-02 (executor wraps the subprocess call). |
| FR-09 | Health checks & observability — `GET /healthz` (no auth) returns 200 `{status: ok}`; `GET /readyz` (no auth) requires DB reachable AND `alembic current == head`, else 503 with body identifying which check failed; `GET /v1/metrics` (scope `admin`) returns task counts by status, latency percentiles, rate-limit rejections; `/readyz` must fail-closed on stale migration; spec lines 152–160. | observability / operability | The `alembic current == head` check is the single defense against R9 ("deployed new code, forgot to migrate"). DB + migration is an AND, not an OR — splitting them would let a half-broken stack pass. Metrics scope is `admin` to keep `count`, `p95`, and `rejected` counters from leaking to `read` callers. | Agent A · REQ | VERIFIED | Source: `SPEC.md` (root) lines 152–160. ACs: AC-9.1–AC-9.4 in `01-requirements/SRS.md` §3. Couples to FR-04 (admin scope guard), FR-07 (migration version check). |
| FR-10 | RFC 7807 error contract — all non-2xx responses carry `Content-Type: application/problem+json`; fixed fields `type` (URI), `title`, `status`, `detail`, `instance`, `correlation_id`; `detail` MUST NOT leak SQL / stack traces / file paths / DB schema; `correlation_id` echoed in `X-Correlation-Id` header and in server logs; error-code mapping: 422 / 401 / 403 / 404 / 409 / 429 / 503 / 500; spec lines 162–168. | contractual / error-shape | RFC 7807 with `correlation_id` is the single point of consistency across the FR-01/02/03/04/05 error surfaces — drift in any one endpoint breaks the contract test. Detail-disclosure is NFR-02 AC-N2.5 (and the R6 mitigation in `01-requirements/SRS.md` §8). | Agent A · REQ | VERIFIED | Source: `SPEC.md` (root) lines 162–168. ACs: AC-10.1–AC-10.5 in `01-requirements/SRS.md` §3. Cross-cuts every other FR. |

### Non-Functional Requirements (NFR — SPEC.md §4)

| FR ID | Spec Description | Intent Class | Decision Framework | Owner | Status | Notes |
|-------|-----------------|--------------|--------------------|-------|--------|-------|
| NFR-01 | Performance & query efficiency — `GET /v1/tasks/{id}` p95 < 30ms on 10k rows; `GET /v1/tasks?limit=50` p95 < 80ms; N+1 is a fail condition (constant statement count per list request, asserted via SQLAlchemy event listener); measured with `pytest-benchmark`; spec lines 177–184. | performance | The event-listener counter is the only verifiable form of "constant statement count" — a heuristic or unit-level test would let the regression slip back. p95 targets are scope-bound to ASGI transport (excludes network). | Agent A · REQ | DRAFT | Source: `SPEC.md` (root) lines 177–184. ACs: AC-N1.1–AC-N1.4 in `01-requirements/SRS.md` §4. Tool coverage: `evaluate_dimension.md` §`### performance (Tier 3)`. |
| NFR-02 | HTTP & data-layer security — no `shell=True` / `eval(` / `exec(` (grep gate, 0 hits); no string-concatenated SQL; API keys SHA-256 + `hmac.compare_digest`; 403 does not leak resource existence; error body has no stack/SQL/paths; CORS default-deny with `TASKQ_CORS_ORIGINS` allowlist; `bandit -r src/` 0 HIGH / 0 MEDIUM; spec lines 185–194. | security | The grep + `bandit` pairing is intentionally redundant — the grep catches patterns `bandit` does not (string-concat SQL is one). CORS default-deny must be the empty-config default, not an opt-in. | Agent A · REQ | DRAFT | Source: `SPEC.md` (root) lines 185–194. ACs: AC-N2.1–AC-N2.7 in `01-requirements/SRS.md` §4. Tool coverage: `evaluate_dimension.md` §`### security (Tier 2)`. |
| NFR-03 | Error handling, transactions, async correctness — per-request transaction boundary (context manager); no bare `except:` / `except Exception: pass`; `asyncio.CancelledError` re-raised; DB failure → `/readyz` 503 (no infinite silent retry); task timeout must kill subprocess; migration failure must rollback DB to prior revision; spec lines 196–204. | reliability | This is the explicit async pitfall envelope. `ast-error-handling` is framework-scored; the precision of async scanning is what this round's testbed is designed to validate (SPEC.md line 483). | Agent A · REQ | DRAFT | Source: `SPEC.md` (root) lines 196–204. ACs: AC-N3.1–AC-N3.6 in `01-requirements/SRS.md` §4. Tool coverage: `evaluate_dimension.md` §`### error_handling (Tier 3)`. |
| NFR-04 | Sensitive data redaction — `stdout_tail` / `stderr_tail` / logs / error bodies redacted with regex `(sk-[A-Za-z0-9_-]{8,}\|token=\S+\|Bearer\s+\S+\|postgres(ql)?://[^\s]+)`; DB connection string (with password) must not appear in logs / error messages / `/v1/metrics`; API key plaintext only emitted once at `key create`; spec lines 206–212. | security | Redaction runs before any write or emit — after-the-fact scrubbing is too late for log files already flushed. The regex must reject `postgres://` *and* `postgresql://`. | Agent A · REQ | DRAFT | Source: `SPEC.md` (root) lines 206–212. ACs: AC-N4.1–AC-N4.3 in `01-requirements/SRS.md` §4. Couples to FR-03 (one-emission invariant), FR-10 (error body). |
| NFR-05 | Documentation coverage — all public functions/classes carry docstrings referencing `[FR-XX]` or `[NFR-XX]` (100% coverage); every API endpoint has `summary` + `description` in OpenAPI schema (asserted via `/openapi.json`); spec lines 214–218. | documentation / maintainability | The `[FR-XX]` / `[NFR-XX]` reference is the only way to make the SRS → code traceability auditable by an automated scanner. Coverage must be 100%, not a target — partial coverage lets orphan code ship. | Agent A · REQ | DRAFT | Source: `SPEC.md` (root) lines 214–218. ACs: AC-N5.1–AC-N5.2 in `01-requirements/SRS.md` §4. Tool coverage: `evaluate_dimension.md` §`### documentation (Tier 3)`. |
| NFR-06 | Architecture layering contract — `.importlinter` exists at project root declaring `api > service > repository > models` (down may import up, up may not import down; `config` and `errors` are independence); **repository is the only layer allowed to import `sqlalchemy`**; `lint-imports` must exit 0; no degraded config (no wildcard `ignore_imports`, no `.importlinter` deletion); spec lines 220–232. | architectural / gate-1 | This is the structural Gate-1 — AC-N6.3 (exit 0) is the binary pass/fail. The forbidden contract ("only `repository/` may import `sqlalchemy`") is enforced by the same tool, not by convention. Downgrading the contract to make `lint-imports` pass is itself a violation (AC-N6.4). | Agent A · REQ | DRAFT | Source: `SPEC.md` (root) lines 220–232. ACs: AC-N6.1–AC-N6.4 in `01-requirements/SRS.md` §4. Tool coverage: `evaluate_dimension.md` §`### architecture_constraints (Tier 1)` — `lint-imports` exit 0 → 100, otherwise → 0. |
| NFR-07 | Dependency & license compliance — runtime deps pinned via `==` in `requirements.txt`; transitive deps locked in `requirements.lock`; allowlist `MIT / BSD-2-Clause / BSD-3-Clause / Apache-2.0 / PSF`; full-dependency-tree scan (`pip-licenses --format=json --with-system`); SBOM artifact at `08-config/SBOM.json` with `name / version / license / direct\|transitive` per dep; spec lines 234–240. | licensing / supply-chain | The `==` pin + lock-pair discipline is the only way to make deployment reproducible. `pip-licenses --with-system` is the narrow, complete-tree scan — anything that skips system packages silently misses transitive licenses. SBOM artifact existence is *separate* from `scancode` scoring and must be verified independently. | Agent A · REQ | DRAFT | Source: `SPEC.md` (root) lines 234–240. ACs: AC-N7.1–AC-N7.4 in `01-requirements/SRS.md` §4. Tool coverage: `evaluate_dimension.md` §`### license_compliance (Tier 1)`. AC-N7.4 needs an independent Phase-3 verification (not just `scancode` score). |
| NFR-08 | Mutation testing — `.methodology/harness_config.json` sets `features.mutation_testing: true`; mutation score ≥ 70; scope limited to `service/` and `repository/` with rationale recorded in `harness_config.json`; spec lines 242–247. | mutation / test-quality | The scope-limit rationale (runtime budget) is what justifies excluding `api/` — without it, the limiter is just a way to game the score. Score threshold of 70 is below the framework default to leave headroom for legitimate uncovered mutants. | Agent A · REQ | DRAFT | Source: `SPEC.md` (root) lines 242–247. ACs: AC-N8.1–AC-N8.3 in `01-requirements/SRS.md` §4. Tool coverage: `evaluate_dimension.md` §`### mutation_testing (Tier 1)`. |
| NFR-09 | Test-verification honesty (zero-skip iron rule) — no FR/NFR test may be `pytest.skip` / `skipif` / `xfail` / zero-assert stub; `pytest 03-development/tests -q` skipped count must be 0; every test ≥ 1 `assert` (`zero_assert == 0`); no `--ignore` / `-k` / `--deselect` / `collect_ignore` exclusion tricks; FR-07 migration tested against real SQLite file; `TRACEABILITY_MATRIX.md` `VERIFIED` only when test actually ran and passed; spec lines 249–257. | test-quality / integrity | AC-N9.4 (anti-fake clause) is what prevents the obvious escape hatch — flagging a test as `skip` or removing it from `testpaths`. The `TRACEABILITY_MATRIX.md` `VERIFIED` cell is the audit point: an upstream matrix saying `VERIFIED` for a downstream test that was skipped is the failure this NFR exists to catch. | Agent A · REQ | DRAFT | Source: `SPEC.md` (root) lines 249–257. ACs: AC-N9.1–AC-N9.6 in `01-requirements/SRS.md` §4. Tool coverage: `evaluate_dimension.md` §`### test_assertion_quality (Tier 2)`. STRUCTURAL GATE for `01-requirements/TRACEABILITY_MATRIX.md`. |
| NFR-10 | Integration coverage — `03-development/tests/integration/` line coverage ≥ 80% of source tree; integration tests use `httpx.AsyncClient(transport=ASGITransport(app))`, never direct handler calls; must cover CRUD chain, each error code (401/403/404/409/422/429/503), migration round-trip, rate-limit trigger+recovery, graceful drain; spec lines 259–264. | integration / coverage | The 80% threshold is scope-broad (whole source tree, not just `service/`), so the integration suite cannot become a thin E2E shell. `httpx.AsyncClient(transport=ASGITransport(app))` is the only configuration that exercises the ASGI middleware chain in-process. | Agent A · REQ | DRAFT | Source: `SPEC.md` (root) lines 259–264. ACs: AC-N10.1–AC-N10.3 in `01-requirements/SRS.md` §4. Tool coverage: `evaluate_dimension.md` §`### integration_coverage (Tier 2)`. |
| NFR-11 | Readability — project MI (LLOC-weighted) ≥ 80; function CC ≤ 10; single file ≤ 400 lines; single directory ≤ 15 files; each API handler ≤ 40 lines (business logic must sink into `service/`); spec lines 266–271. | maintainability | The handler ≤ 40 lines rule is what forces a real split between `api/` and `service/` — without it, handlers absorb logic and NFR-11 reads a tautology. `radon mi` averages, so a single huge file can still keep the average high; the per-file/per-function caps are the real gates. | Agent A · REQ | DRAFT | Source: `SPEC.md` (root) lines 266–271. ACs: AC-N11.1–AC-N11.5 in `01-requirements/SRS.md` §4. Tool coverage: `evaluate_dimension.md` §`### readability (Tier 3)` (`radon-mi`). |
| NFR-12 | System verification target — `Makefile` `verify-system` chains: (1) `alembic upgrade head`, (2) full test suite, (3) service start + `/healthz` + `/readyz` smoke, (4) `downgrade base` then `upgrade head` (round-trip); must exit 0 and print `verify-system: PASS` to stdout; spec lines 273–281. | deployability / gate-2+3+4 | The step ordering (round-trip last, not interleaved) is what proves migration reversibility under the same boot cycle. Stdout grep for `verify-system: PASS` is a tight, parseable success marker — anything less specific lets a noisy suite mask failures. | Agent A · REQ | DRAFT | Source: `SPEC.md` (root) lines 273–281. ACs: AC-N12.1–AC-N12.2 in `01-requirements/SRS.md` §4. Tool coverage: `evaluate_dimension.md` §`### execute_verification_target (Tier 1)` — Gates 2/3/4. |

## Forward References (legal filenames only)

Phase 1 deliverables owned by this stage:
- `01-requirements/SRS.md` (APPROVED — see FR Block for machine-readable form)
- `01-requirements/SPEC_TRACKING.md` (this file)
- `01-requirements/TRACEABILITY_MATRIX.md` (companion, machine-refreshed)
- `01-requirements/TEST_INVENTORY.yaml`

Downstream phase hand-offs referenced from this matrix:
- Phase 2 architecture: `02-architecture/ADR.md`, `02-architecture/SAD.md`, `02-architecture/TEST_SPEC.md`
- Phase 4 testing: `04-testing/TEST_PLAN.md`, `04-testing/TEST_RESULTS.md`
- Phase 5 verification: `05-verification/BASELINE.md`, `05-verification/VERIFICATION_REPORT.md`
- Phase 6 quality: `06-quality/FINAL_SIGN_OFF.md`, `06-quality/QUALITY_REPORT.md`, `06-quality/RELEASE_NOTES.md`, `06-quality/quality_manifest.json`
- Phase 7 risk: `07-risk/RISK_MITIGATION_PLANS.md`, `07-risk/RISK_REGISTER.md`, `07-risk/RISK_STATUS_REPORT.md`
- Phase 8 config: `08-config/CONFIG_RECORDS.md`, `08-config/RELEASE_CHECKLIST.md`, `08-config/SBOM.json`

## Completeness check

- FR coverage: 10 / 10 (FR-01 through FR-10) — matches SRS.md §3.
- NFR coverage: 12 / 12 (NFR-01 through NFR-12) — matches SRS.md §4.
- Every row's Source column points at the root-level canonical spec (bare `SPEC.md`, no directory prefix) — passes the canonical-spec-path guard.
- Every FR / NFR row carries: Spec Description, Intent Class, Decision Framework, Owner, Status, Notes — satisfies STANDARD-template column requirement.
- All Notes cells cross-link to `01-requirements/SRS.md` AC IDs and, where applicable, to `01-requirements/TRACEABILITY_MATRIX.md` (companion deliverable).

## Update log

| Date | Change | By |
|------|--------|----|
| 2026-09-02 | Initial creation — populated 10 FRs + 12 NFRs from `01-requirements/SRS.md` (APPROVED); set Source column to root `SPEC.md` per canonical-spec-path guard; aligned Forward References with legal filenames per stage. | Agent A (REQUIREMENTS_ENGINEER) |
